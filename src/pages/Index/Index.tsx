import { FlexContainer } from "../../Ui/UiStyle";
import SearchInput from "../../components/SearchInput/SearchInput";

export const Home = () => {
  return (
    <FlexContainer>
      <SearchInput />
    </FlexContainer>
  );
};
